package restaurant.jpa_sequence_server;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = JpaSequenceServerValidateApplication.class, webEnvironment = SpringBootTest.WebEnvironment.NONE)
class JpaSequenceServerValidateApplicationTests {

	@Test
	void contextLoads() {
	}
}
