----
---- ATTENTION CE FICHIER EST GENERE AUTOMATIQUEMENT !
----

-- ===========================================================================================
--   Description		:	Insertion des valeurs de listes statiques.
-- ===========================================================================================

/* Insertion dans la table ASSIETTE. */
:r .\ASSIETTE.insert.sql

/* Insertion dans la table CATEGORIE_PLAT. */
:r .\CATEGORIE_PLAT.insert.sql

/* Insertion dans la table REGION. */
:r .\REGION.insert.sql

/* Insertion dans la table CATEGORIE_PLAT_REGION. */
:r .\CATEGORIE_PLAT_REGION.insert.sql

/* Insertion dans la table COUVERT. */
:r .\COUVERT.insert.sql

/* Insertion dans la table DEPARTEMENT. */
:r .\DEPARTEMENT.insert.sql

/* Insertion dans la table LIEU. */
:r .\LIEU.insert.sql

/* Insertion dans la table PERSONNE. */
:r .\PERSONNE.insert.sql

/* Insertion dans la table EMPLOYE. */
:r .\EMPLOYE.insert.sql

