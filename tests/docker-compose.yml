services:
  db:
    image: postgres:latest
    ports:
      - 5962:5432
    environment:
      - POSTGRES_PASSWORD=demo
      - POSTGRES_USER=demo
      - POSTGRES_DB=demo
    volumes:
      - ./output/pg/src:/docker-entrypoint-initdb.d
